# Legacy Results Directory

This directory contains historical experiment outputs from the original repository.

New runs from the cleaned, reproducible pipeline are written to `runs/<run_name>/` to avoid mixing generated artifacts with these legacy results.
